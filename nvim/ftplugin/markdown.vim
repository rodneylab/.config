" turn on spell check and set language
setlocal spell spelllang=en_gb

" wrap to end of word rather than split word across lines
set linebreak

" text words which cross column 80 get wrapped to next line, when editing
set textwidth=80
